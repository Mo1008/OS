# 进程
==**资源分配**==的最小单位
## 进程控制
### 概念
即进程的状态转换
### 原子操作
进程转换使用**原子操作**实现
原因：避免某些关键数据在状态转换过程中出现信息不统一
####  实现方式
使用**关中断指令**和**开中断指令**实现
关中断指令：使cpu不再检查中断信号
开中断指令：使cpu恢复检查中断信号

### 创建进程
1. 申请空PCB
2. 为新进程分配空间
3. 初始化PCB
4. 将PCB插入就绪队列
#### 终止进程
1. 在PCB队列中找到对应的PCB
2. 若正在运行，立即剥夺cpu，将cpu资源分配给其他进程
3. 终止所有子进程
4. 将该进程资源归还父进程或操作系统
5. 删除pcb
## 进程通信
两个进程间的数据交互
需要**OS**的支持
原因：进程使分配系统资源的单位，各进程拥有**内存地址空间相互独立**
### 共享存储
#### 基于存储区的共享
OS划出一块共享存储区，内部的数据形式、存放位置都由**通信进程**控制，而非OS
**速度快**、**自由度高 **
<figure>
	<img src="Pasted image 20260416154212.png" alt="基于存储区的共享"> 
	<figcaption align="center"><b>图 1：</b> 基于存储区的共享</figcaption> 
</figure>

#### 基于数据结构
**速度慢**、**自由度低**

<figure>
	<img src="Pasted image 20260416155151.png" alt="基于数据结构的共享"> 
	<figcaption align="center"><b>图 2： 基于数据结构的共享</b> </figcaption> 
</figure>
### 消息传递

### 管道通信
**半双工通信**，某一时间段内只能实现单向传输
pipe写满时，写进程被阻塞，直至读进程读取
pipe为空时，读进程被阻塞，直至写进程写入
<figure>
	<img src="Pasted image 20260416170146.png" alt="管道通信"> 
	<figcaption align="center"><b>图 3： 管道通信</b> </figcaption> 
</figure>


# 线程
==**调度**==的基本单位
同一进程中的线程共享进程资源
## 实现方式
 