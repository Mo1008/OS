0 A 4 
优先级
## 多级反馈调度(MFQ)
当一个新进程到达，若果优先级高于当前进程，那么当前进程被抢占，被抢占进程进入原队列末尾
较高优先级

### 例子
有3个队列，时间片为1、2、4，A、B、C、D、E的到达时间为0、1、2、3、4、5，运行时间为3、8、4、5、7


```mermaid
gantt
    title 多级反馈队列 (MFQ) 调度图解
    dateFormat  X
    axisFormat  %M

    section 队列 Q1 (q=1)
    A :a1, 0, 1m
    B :b1, after a1, 1m
    C :c1, after b1, 1m
    D :d1, after c1, 1m
    E :e1, after d1, 1m

    section 队列 Q2 (q=2)
    A (完结) :done, a2, after e1, 2m
    B       :b2, after a2, 2m
    C       :c2, after b2, 2m
    D       :d2, after c2, 2m
    E       :e2, after d2, 2m

    section 队列 Q3 (q=4)
    B       :b3, after e2, 4m
    C (完结) :done, c3, after b3, 1m
    D (完结) :done, d3, after c3, 2m
    E       :e3, after d3, 4m
    B (完结) :done, b4, after e3, 1m
    E (完结) :done, e4, after b4, 1m
