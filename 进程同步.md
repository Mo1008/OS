# 临界区
访问共享资源的一段代码，通常是一个变量或者数据结构，每个进程都有临界区代码段
**当一个进程在临界区中时，别的进程不可能在临界区中**
==**互斥**==、==**前进性**==、==**有限等待**==
## 组成
+ 进入区
+ 临界区——用来访问资源的代码
+ 退出区
+ 剩余区
## 原则
+ **空闲让进**
+ **忙则等待**
+ **有限等待**
+ **让权等待**——当进程不能进入临界区是，应立即释放处理机，避免忙等待
## 竞态条件
多个进程/线程同时进入临界区，执行结构与访问发烧与特定顺序有关
## 解决
### 前提
+ **互斥**
+ **前进（进展性）**
+ **有限等待**
### 单标志法

<div style="display: flex; gap: 20px; align-items: flex-start;">
<div style="flex: 1;"> 
<strong>P0 进程:</strong>
<pre style="background: #f6f8fa; padding: 10px; border-radius: 5px;">
while (turn != 0); // ①
critical section; // ②
turn = 1; // ③
remainder section; // ④ </pre> </div>

<div style="flex: 1;"> 
<strong>P1 进程:</strong> 
<pre style="background: #f6f8fa; padding: 10px; border-radius: 5px;">while (turn != 1); // ⑤ //进入区 
critical section; // ⑥ //临界区 
turn = 0; // ⑦ //退出区 
remainder section; // ⑧ //剩余区 </pre> </div>
</di>
### Peterson算法
```
bool flag[2]; //表示是否需要进入临界区
int turn = 0; //表示优先让哪个进程进入

//P0进程：
flag[0] = true;
trun = 1;
while(flag[1] && turn == 1); //等待P1进程
critical section
flag[0] = false;
remander section;
//P1进程：
flag[1] = true;
trun = 0;
while(flag[0] && turn == 0); //等待P0进程
critical section
flag[1] = false;
remainder section;
```

## 硬件层面实现互斥

### TestAndSet指令
```
//bool型共享变量lock表示当前临界区是否被上锁
//true表示被上锁，false表示未被上锁
bool TestAndSet(bool *lock){
	bool old;
	old = *lock;
	*lock = true;
	return old;}
``jj`
TSA指令在代码中使用：
```
while(TestAndSet(&lock))；
critical section
lock = false;
剩余代码
```
**优点：** 实现简单，适用于多处理机环境
```

## 互斥锁
### 自旋锁
aquire()获得锁，release()释放锁
```
aquire(){
	while(!avalibal);
	avalibale = false;
}

release(){
avalibale = true;
}
```
**缺点：** 忙等待，不适合单处理机系统
**优点：** 不用频繁切换上下文，适合多处理机系统，若上锁时间短，等待代价低
## 信号量
用户进程通过os提供的==一对原语==来对==信号量==进行操作
==信号量==是一个变量，可以用来表示==系统中某种资源的数量==
==一对原语==：==wait(S)==和==signal(S)==，可简写为P()和V()
### 整型信号量
用一个整数来表示系统中某种资源的数量
```
int S = 1;

void wait(int S){  //相当于“进入”临界区
	while(S <= 0); //如果资源不足，则忙等待
	S -= 1;        //如果资源充足，则减少一个名额
}

void signal(int S){
	S += 1;
}

进程P：
···
wait(S);
操作
signal(S);
```
### 记录型信号量
```
typedef struct{
	int value;         //剩余资源量
	struct process *L; //等待队列
}semaphore
```

```
//进程需要使用资源时，通过wait原语申请
void wait(struct semaphore S){
	S.value--;
	if(S.value < 0){
		block(S.L);  //使进程由就绪态变为阻塞态
	}
}

//进程使用完资源后，通过signal原语释放
void signal(semaphore S){
	S.value++;
	if(s.value <= 0){ //S.value <= 0说明有进程正在等待，而此时空出一个资源了，所有进程唤醒
		wakeup(S.L);  //使进程由阻塞态变为就绪态
	}
}
```

### 用信号量机制实现进程互斥
1. 设置==互斥信号量mutex==，初始值为1
2. 进入区P(mutex)——申请资源
3. 退出区V(mutex)——释放资源
```
semophore mutex(1, null);

P1(){
	······
	P(mutex);
	critical section
	V(mutex);
	······
}

P2(){
	······
	P(mutex);
	critical section
	V(mutex);
	······
}
```
### 用信号量机制实现进程同步
1. 分析在何位置需要实现同步
2. 设置==同步信号量==S，初始为==0==
3. ==在前置操作之后执行V(S)==
4. ==在后置操作之前执行P(S)==
```
semophore S = 0;

//假如代码4需要在代码1、代码2后执行
P1(){
	代码1;
	代码2;
	V(S);  //初始时设置S为0，即无空闲资源，故代码4只有在V(S)释放资源后才可能执行
	代码3;
}

P2(){
	P(S);
	代码4;
	代码5;
	代码6;
}
```
