# CPU状态
当前cpu在执行哪类程序决定了cpu的态
目的：保护OS

|  管理程序  |   用户程序    |
| :----: | :-------: |
| 管理操作系统 | 使用资源，提出申请 |
| 控制程序运行 |    被控制    |

## cpu中的重要寄存器
**PC**：
**指令寄存器IR**：
**程序状态字寄存器**：
**通用寄存器**：
**控制寄存器**：

### risc-v中还有

## CPU特权级切换
### 场景
* 系统提供的**系统调用**
* 触发**异常**
* 接收到**中断**
切换后要有syscall返回指令，恢复切换前的状态

### 中断
关中断 -> 保存断点 -> 引出中断服务程序 -> 保存现场和屏蔽字 -> 开中断（可能有**更高级的中断**） -> 执行中断服务程序 -> 关中断 -> 恢复现场和屏蔽字 -> 