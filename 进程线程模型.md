# 进程
一个程序在数据集上的动态执行过程
组成：**PCB（进程控制块）**、**数据段**、**程序段**
状态：创建、就绪、运行、阻塞、终止
## 阻塞与挂起
|      |   阻塞   |             挂起             |
| :--: | :----: | :------------------------: |
|  原因  |  主动暂停  |      因为某种原因暂时不能运行<br>      |
| 暂停时  | 进入阻塞队列 | 不进入阻塞队列，保存在磁盘中，不占用CPU和内存资源 |
| 对应操作 |   唤醒   |             激活             |
## Fork
在调用fork()后通常都会调用exec()，原因：
1. 在fork()操作中内存复制没有作用，在刚fork时父子进程共享内存空间，当子进程有新代码时再重新申请内存
2. 子进程可能打开/关闭父进程的文件和网络链接